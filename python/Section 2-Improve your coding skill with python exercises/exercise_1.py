# TODO generate random number

import random #=> random is an module which is used to gnerate the random number
#module is a collection of code which contains class,object,method,fuction,variable,values
#example is "random.py" is file if you import this file in "doc.py" that called module
print(random.randint(0,10)) #randint is a function that used to generate the random number between the ranges
